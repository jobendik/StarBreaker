{
  "name": "void-drift",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "VOID DRIFT — a fast-paced survival arena shooter rendered on HTML canvas.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.19"
  }
}
